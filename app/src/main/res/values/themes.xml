<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.QuizAppSqLite" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/bgColor</item>
        <item name="colorPrimaryVariant">@color/bgColor</item>
        <item name="colorOnPrimary">@color/bgColor</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/btnNextColor</item>
        <item name="colorSecondaryVariant">@color/btnNextColor</item>
        <item name="colorOnSecondary">@color/btnNextColor</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
    </style>
</resources>